<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.iot.mapper.IotAutomationRecordMapper">

    <!-- 自动化记录结果映射 -->
    <resultMap type="IotAutomationRecord" id="IotAutomationRecordResult">
        <result property="recordId" column="record_id"/>
        <result property="protocolId" column="protocol_id"/>
        <result property="controlType" column="control_type"/>
        <result property="deviceType" column="device_type"/>
        <result property="operationStatus" column="operation_status"/>
        <result property="sensorData" column="sensor_data"/>
        <result property="controlReason" column="control_reason"/>
        <result property="thresholdSettings" column="threshold_settings"/>
        <result property="executeResult" column="execute_result"/>
        <result property="errorMessage" column="error_message"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 查询字段SQL片段 -->
    <sql id="selectRecordVo">
        select record_id, protocol_id, control_type, device_type, operation_status,
               sensor_data, control_reason, threshold_settings, execute_result, error_message,
               create_time, update_time
        from iot_automation_record
    </sql>

    <!-- 新增自动化记录 -->
    <insert id="insertRecord" parameterType="IotAutomationRecord" useGeneratedKeys="true" keyProperty="recordId">
        insert into iot_automation_record (
            record_id, protocol_id, control_type, device_type, operation_status,
            sensor_data, control_reason, threshold_settings, execute_result, error_message,
            create_time, update_time
        ) values (
                     #{recordId}, #{protocolId}, #{controlType}, #{deviceType}, #{operationStatus},
                     #{sensorData}, #{controlReason}, #{thresholdSettings}, #{executeResult}, #{errorMessage},
                     #{createTime}, #{updateTime}
                 )
    </insert>

    <!-- 查询自动化记录列表，支持条件查询 -->
    <select id="selectRecordList" parameterType="IotAutomationRecord" resultMap="IotAutomationRecordResult">
        <include refid="selectRecordVo"/>
        <where>
            <if test="protocolId != null"> and protocol_id = #{protocolId}</if>
            <if test="controlType != null and controlType != ''"> and control_type = #{controlType}</if>
            <if test="deviceType != null and deviceType != ''"> and device_type = #{deviceType}</if>
            <if test="executeResult != null and executeResult != ''"> and execute_result = #{executeResult}</if>
            <!-- 支持时间范围查询 -->
            <if test="params.beginTime != null and params.beginTime != ''">
                and date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
        </where>
        order by create_time desc
    </select>

    <!-- 根据ID查询自动化记录 -->
    <select id="selectRecordById" parameterType="Long" resultMap="IotAutomationRecordResult">
        <include refid="selectRecordVo"/>
        where record_id = #{recordId}
    </select>

    <!-- 根据协议ID查询最新记录 -->
    <select id="selectLatestRecordByProtocolId" parameterType="Long" resultMap="IotAutomationRecordResult">
        <include refid="selectRecordVo"/>
        where protocol_id = #{protocolId}
        order by create_time desc
        limit 1
    </select>

    <!-- 批量插入自动化记录 -->
    <insert id="batchInsertRecord" parameterType="java.util.List">
        insert into iot_automation_record (
        protocol_id, control_type, device_type, operation_status,
        sensor_data, control_reason, threshold_settings, execute_result, error_message,
        create_time, update_time
        ) values
        <foreach collection="records" item="item" index="index" separator=",">
            (
            #{item.protocolId}, #{item.controlType}, #{item.deviceType}, #{item.operationStatus},
            #{item.sensorData}, #{item.controlReason}, #{item.thresholdSettings}, #{item.executeResult}, #{item.errorMessage},
            #{item.createTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

</mapper>